# Gate Verification Archive Index

**日期**: 2026-04-13
**作用**: 作为根仓库 `reports/gates/` 迁移到 `reports/archive/gate_verification/` 的首个受控索引文件。
**状态**: 已建立受控索引，核心历史摘要已完成 admission，剩余新快照待审计

---

## 当前真实状态

| 项目 | 当前值 | 说明 |
|------|--------|------|
| 工作区 `reports/gates/` 文件数 | 0 | 主目录已从工作区清空 |
| Git 中 `reports/gates` 已跟踪文件数 | 0 | 历史摘要删除已进入提交历史 |
| `reports/archive/gate_verification/` 已跟踪文件数 | 235 | canonical 归档已部分入库 |
| `reports/archive/gate_verification/` 未跟踪文件数 | 59 | 其中 58 份为新增真实快照，1 份为模板文件 |
| 重复来源路径 | 3 处 | `reports/archive/gates/` 与根目录平铺 CSV 两份 |

结论：

1. 根目录 `reports/gates/*` 的历史摘要删除已经进入提交历史，不能再把当前问题描述成“删除待处理”。
2. 迁移已经进入第二阶段：核心历史摘要已入库到 `reports/archive/gate_verification/`，剩余文件已缩小到 58 个新增真实快照和 1 个模板文件。
3. 当前主要风险不再是“缺失对应物”，而是“新增快照与重复来源路径尚未定性”。
4. 两个历史 CSV 聚合文件已补齐到 canonical 目录，根目录平铺副本应视为重复来源，不再进入 Git。

---

## 迁移原则

后续所有根仓库门禁证据迁移，统一遵循以下顺序：

1. **先登记**
   在索引中记录来源目录、归档目录、文件族、缺失项和验证命令。

2. **再迁移**
   只把已经确认存在归档对应物的文件族纳入迁移提交；必要时补齐缺失归档。

3. **后删除**
   只有在索引、归档文件和删除清单三者一致时，才允许提交 `reports/gates/*` 的删除。

---

## 已确认的归档文件族

下列文件族已经在本地 `reports/archive/gate_verification/` 中找到对应归档：

| 文件族 | 归档状态 | 说明 |
|------|------|------|
| `backend_verify_*` | 已存在 | 同时包含 `.md` 和 `.log` |
| `final_decision_consistency_*` | 已存在 | 同时包含 `.md` 和 `.log` |
| `metrics_daily_snapshot_*` | 已存在 | 按日快照已归档 |
| `metrics_trend_7d_*` | 已存在 | 趋势摘要已归档 |
| `minimax_upstream_daily_snapshot_*` | 已存在 | 日快照已归档 |
| `minimax_upstream_smoke_*` | 已存在 | 同时包含 `.md` 和 `.log` |
| `minimax_upstream_trend_7d_*` | 已存在 | 趋势摘要已归档 |
| `staging_real_readiness_*` | 已存在 | 同时包含 `.md` 和 `.log` |
| `staging_release_pipeline_*` | 已存在 | 同时包含 `.md` 和 `.log` |
| `staging_token_go_evidence_autofill_*` | 已存在 | 归档中同时保留摘要和日志 |
| `superpowers_release_pipeline_*` | 已存在 | 同时包含 `.md` 和 `.log` |
| `superpowers_stage_validation_*` | 已存在 | 同时包含 `.md`、`.log` 和步骤输出 |
| `token_runtime_readiness_*` | 已存在 | 当前本地归档共 164 个相关文件 |
| `token_runtime_smoke_*` | 已存在 | 包含 `.log`、`.log.audit.json`、`.log.issue.json`、`.log.server` |
| `token_runtime_bin_*` | 已存在 | 本地归档保留了二进制快照，是否继续纳入仓库需单独决策 |

---

## 已补齐的缺口

以下文件此前在 staged deletion 中，但未在 `reports/archive/gate_verification/` 找到同名对应物；本轮已按 `HEAD:reports/gates/*` 内容补齐归档：

- `metrics_daily_snapshots.csv`
- `minimax_upstream_daily_snapshots.csv`

补齐说明：

1. 归档版本使用当前 Git `HEAD` 中对应 CSV 的内容。
2. CSV 中仍保留对历史 `reports/gates/*.md` 路径的引用，这些引用属于历史证据上下文，不在补档时重写。

---

## 归档与删除的关系说明

`reports/archive/gate_verification/` 当前文件数显著大于 `reports/gates` 的 staged deletion 数，原因是归档目录不仅保存摘要 `.md`，还保存了：

- 执行日志 `.log`
- 分步骤输出 `step-*.out.log`
- 审计副产物 `.log.audit.json`
- 问题副产物 `.log.issue.json`
- 服务输出 `.log.server`
- 一部分二进制快照 `token_runtime_bin_*`

因此迁移关系不是简单的一对一“移动文件”，而是从主目录摘要文件扩展到完整证据包的收口。

本轮分类原则：

- **纳入 Git 的有效归档**：`*.md`、`*.csv`、索引文件。
- **仅保留为本地审计留档**：`*.log`、`*.out.log`、`*.log.audit.json`、`*.log.issue.json`、`*.log.server`、`token_runtime_bin_*`。

对应忽略规则已加入仓库根 `.gitignore`，用于压缩工作区噪音，但不改变这些原始证据在本地的保留状态。

---

## 已完成 admission 的核心批次

当前已入库的 canonical 归档覆盖以下批次：

1. 指标与上游摘要：`metrics_*`、`minimax_upstream_*`
2. 决策摘要：`final_decision_consistency_*`
3. 历史后端验证摘要：`backend_verify_2026-04-11_091249.md`
4. 阶段验证与发布流水：`staging_*`、`superpowers_release_pipeline_*`、已存在历史对应物的 `superpowers_stage_validation_*`
5. Token runtime 历史 readiness 摘要：截至 `2026-03-31`

## 待处理项

以下内容仍未完成正式定性：

- `backend_verify_*` 在 `2026-04-11` 到 `2026-04-13` 的新增 10 份摘要
- `superpowers_stage_validation_*` 在 `2026-04-11` 到 `2026-04-13` 的新增 6 份摘要
- `token_runtime_readiness_*` 在 `2026-04-11` 到 `2026-04-13` 的新增 16 份摘要
- `tok005_dryrun_*` 的新增 17 份摘要
- `tok006_gate_bundle_*` 的新增 9 份摘要
- `tok006_release_decision_onepager_template_v1_2026-03-30.md` 模板文件
- `reports/archive/gates/` 与 `reports/archive/*.csv` 的重复来源路径

处理规则：

1. 只有真实执行摘要才允许 admission。
2. 模板文件和重复来源路径不进入 canonical 归档。
3. 新增快照需先做内容审计，再决定 admission 或降级为本地留档。

## 下一步执行要求

下一批处理必须完成以下动作后才能提交：

1. 对剩余 62 个未跟踪摘要按文件族做真实性审计。
2. 将 `reports/archive/gates/` 与根目录平铺 CSV 视为重复来源，不再作为 admission 候选。
3. 对模板文件建立“无效历史模板”清单，避免误入 canonical 归档。
4. 每批 admission 前继续使用 `git diff --check` 和文件族交集校验，防止再次出现“先删后补”的提交顺序错误。

---

## 建议验证命令

```bash
find reports/gates -maxdepth 1 -type f | wc -l
git diff --cached --name-only -- reports/gates | wc -l
find reports/archive/gate_verification -maxdepth 1 -type f | wc -l
```

```bash
archive_list=$(mktemp)
delete_list=$(mktemp)
find reports/archive/gate_verification -maxdepth 1 -type f -printf "%f\n" | sort -u > "$archive_list"
git diff --cached --name-only -- reports/gates | xargs -n1 basename | sort -u > "$delete_list"
comm -23 "$delete_list" "$archive_list"
rm -f "$archive_list" "$delete_list"
```
